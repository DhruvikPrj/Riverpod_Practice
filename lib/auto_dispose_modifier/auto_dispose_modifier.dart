import 'package:flutter/material.dart';

class AutoDisposemodifier extends StatelessWidget {
  const AutoDisposemodifier({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
